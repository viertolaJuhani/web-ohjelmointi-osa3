{
  "name": "web-ohjelmointi-osa3",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://gitlab.utu.fi/javier/web-ohjelmointi-osa3.git"
  },
  "license": "ISC",
  "author": "Juhani Viertola",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../web-ohjelmointi/osa2/ && npm run build && cp -r dist ../web-ohjelmointi-osa3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
